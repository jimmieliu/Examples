#!/usr/bin/env python
from distutils.core import setup, Extension
MOD = 'cpsr'

module1 = Extension(MOD,
                    sources = ['cppparser.cpp'])

setup (name = MOD,
       version = '1.0',
       description = 'cpp parser',
       ext_modules = [module1])
